
 export default {
     "settings": {
    "default": {
      "authToken": {
        "type": "secret",
        "defaultValue": "",
        "required": true
      },
      "subdomain": {
        "type": "string",
        "defaultValue": "",
        "description": "The subdomain field that will be included in the API when we make a request"
      }
    }
  }
}
